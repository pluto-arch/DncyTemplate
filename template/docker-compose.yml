version: '3.4'

services:
  dncytemplate.api:
    image: ${DOCKER_REGISTRY-}dncytemplateapi
    build:
      context: .
      dockerfile: ./entry/DncyTemplate.Api/Dockerfile

  dncytemplate.mvc:
    image: ${DOCKER_REGISTRY-}dncytemplatemvc
    build:
      context: .
      dockerfile: ./entry/DncyTemplate.Mvc/Dockerfile

  dncytemplate.job:
    image: ${DOCKER_REGISTRY-}dncytemplatejob
    build:
      context: .
      dockerfile: ./entry/DncyTemplate.Job/Dockerfile

